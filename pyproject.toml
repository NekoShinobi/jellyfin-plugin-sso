[project]
name = "jellyfin-sso-tools"
version = "0.0.0"
description = "Development tooling for the Jellyfin SSO plugin"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = ["zensical==0.0.64"]

[tool.uv]
package = false
default-groups = ["docs"]
